{
  "buildCommand": "mkdir -p dist && cp index.html dist/index.html && mkdir -p dist/logo-copy",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
